{
  "version": "rules.v5.0",
  "files": [
    "lock-palace.json",
    "scoring.json",
    "qtype-rules.json",
    "fengshui-rules.json"
  ],
  "note": "This index keeps rule data discoverable. V5.0 applies qtype weights while marking the values as pending calibration."
}
