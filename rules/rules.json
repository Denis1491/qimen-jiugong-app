{
  "version": "rules.v4.3",
  "files": [
    "lock-palace.json",
    "scoring.json",
    "qtype-rules.json",
    "fengshui-rules.json"
  ],
  "note": "This index keeps rule data discoverable. The app keeps the current scoring behavior stable and does not apply qtype weights until sources are confirmed."
}
